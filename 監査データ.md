システム診断レポート（2026-03-14 15:00 JST）
1. プロジェクト概要
技術スタック: React + Vite + TypeScript + Tailwind CSS + Supabase（外部プロジェクト）
Supabase Project ID: tqcgbsnoiarnawnppwia
公開URL: https://threads-genius-ai.lovable.app
目的: Threads（Meta）への自動投稿・AI自動返信・定型文返信を管理するSaaS
2. Edge Function ログ状況
❌ auto-scheduler — 認証失敗で毎回拒否
2026-03-14T06:00:01Z ERROR Auto-scheduler auth failed - request rejected
2026-03-14T05:55:01Z ERROR Auto-scheduler auth failed - request rejected
requireInternalRequest() がx-cron-secretまたはservice_role keyを要求
cronジョブがCRON_SECRETまたはservice_role keyを正しく送信していない
cron_job_status ビューは 空（[]） → cronジョブが未設定 or ビューが参照できない
⚠️ auto-post-generator — processing_statusカラム不在エラー
2026-03-14T06:00:02Z ERROR Failed to claim auto-post config: column auto_post_configs.processing_status does not exist (42703)
auto_post_configs テーブルには processing_status カラムが定義されている（types.tsで確認済み）
PostgRESTスキーマキャッシュが古い可能性 → NOTIFY pgrst, 'reload schema' が必要
テンプレート投稿は一部動作しているが、多くのペルソナが「not found or inactive」
✅ check-replies — 動作中だが大半のペルソナをスキップ
⏭️ トークン期限切れスキップ: 14みく (期限: 1970-01-01)
Skipping persona xxx - no access token
⏭️ レート制限中スキップ: muchi2588【地雷5】
🚨 緊急停止：異常な処理数を検出しました
有効なトークンを持つペルソナ: カミンチュ、みるく、あみみ のみ3件処理
いずれも No recent posts found で新規リプライなし
「緊急停止」ログが出ている = ペルソナ数が多すぎてガードに引っかかる
✅ process-unhandled-replies — 正常動作
🔧 未処理リプライの再処理開始...
📋 取得件数: 0, リトライ: 0, フィルタ後: 0
✅ 未処理リプライなし
❌ threads-post — ログなし（最近呼ばれていない）
❌ threads-auto-reply — ログなし（最近呼ばれていない）
❌ generate-auto-reply — ログなし（最近呼ばれていない）
❌ threads-webhook — ログなし（Webhookが来ていない）
❌ self-reply-processor — ログなし
⚠️ retrieve-secret — JWT検証失敗
❌ JWT validation failed: invalid JWT: token is malformed: token contains an invalid number of segments
check-token-healthから呼ばれた際にservice_role keyではなく不正なトークンが渡された
3. データベース状態
ペルソナ（personas WHERE is_active = true）
状態	件数（概算）
has_token=true & token有効（2026-04以降）	~15件（OAuth済み: Leo, Maya, mikandani等）
has_token=true & token期限切れ（1970-01-01）	~10件（14みく, 33つむぎ等）
has_token=false	~25件
is_rate_limited=true	5件（muchi系, せいな）
投稿統計（posts GROUP BY status）
status	件数
failed	20,094
published	17,952
scheduled	163
processing	3（スタック）
リプライ統計（thread_replies GROUP BY reply_status, auto_reply_sent）
reply_status	auto_reply_sent	件数
failed	false	116,421
completed	true	92,614
sent	true	80,913
failed	true	11,298
pending	true	4,469
scheduled	false	190
scheduled	true	12
自動投稿設定（auto_post_configs WHERE is_active = true）
全件の next_run_at が 2025-10〜2026-03-04 の古い日時のまま → スケジュールが更新されていない
全件 processing_status: idle
auto-schedulerが認証失敗で動いていないため、next_run_atが更新されない
ランダム投稿設定（random_post_configs WHERE is_active = true）
3件アクティブ、next_run_at も古い or 今日
Cronジョブ（cron_job_status ビュー）
結果が空（[]） → cronジョブが設定されていないか、ビューの参照権限がない
Webhook設定（webhook_settings WHERE is_active = true）
結果が空（[]） → アクティブなWebhook設定なし → threads-webhookが呼ばれない原因
リプライチェック設定（reply_check_settings）
20件以上アクティブだが、last_check_atは2026-03-03が最新（11日前で停止）
4. Supabase Secrets（Edge Functions用）
シークレット名	状態
ENABLE_TEST_FUNCTIONS	設定済み
LOVABLE_API_KEY	設定済み（削除不可）
RESEND_API_KEY	設定済み
CRON_SECRET	未確認（Lovable secretsには未登録 → Supabase側で直接設定されている可能性）
ENCRYPTION_KEY	未確認（同上）
GEMINI_API_KEY	未確認（同上）
5. 共有モジュール
_shared/auth.ts — requireInternalRequest()
CRON_SECRET ヘッダーまたは SUPABASE_SERVICE_ROLE_KEY のBearer/apikeyマッチを要求
auto-schedulerがこれで毎回拒否されている
_shared/crypto.ts
AES-256-GCM（raw key + PBKDF2フォールバック）で暗号化/復号
ENCRYPTION_KEY 環境変数が必須
isEncrypted(): THAA開頭は平文判定、40文字以下も平文判定
6. 根本的な問題のまとめ
#	問題	影響範囲	原因
1	auto-scheduler認証失敗	全自動投稿のスケジュール管理	cronジョブが正しい認証ヘッダーを送っていない or CRON_SECRET未設定
2	processing_statusカラム不在（42703）	auto-post-generatorのclaim処理	PostgRESTスキーマキャッシュ未更新
3	大半のペルソナがトークン期限切れ/未設定	check-replies, 自動返信, 手動投稿	token_expires_at=1970-01-01 or threads_access_token=null
4	Webhook設定が0件	threads-webhookによるリアルタイム返信検知	webhook_settingsにアクティブな設定なし
5	cronジョブ未確認	auto-scheduler, check-replies, process-unhandled-repliesの定期実行	cron_job_statusが空
6	threads-post/threads-auto-reply/generate-auto-reply未実行	投稿と返信	上流（auto-scheduler/check-replies）が機能していないため下流も未実行
7	retrieve-secretのJWT検証失敗	check-token-healthからのトークン取得	service間呼び出しで不正なトークンが渡されている
7. supabase/config.toml の設定
全Edge Functionが verify_jwt = false に設定済み
テスト用関数含め41個のEdge Functionが定義
8. 最近の投稿（直近20件）
ステータス: scheduledが複数（投稿されず放置）
published_at: null（投稿実行されていない）
threads-postが呼ばれていないため
9. 最近のリプライ（直近20件）
ペルソナ カミンチュ (230a3c9f) のみがbot_generated webhook_auto_replyとして sent 状態
ai_response は全てnull（AIによるレスポンス生成なしで自動送信されている）
🌺 のみの単純なリプライが13秒間隔で大量送信されている