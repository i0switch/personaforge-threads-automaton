元不具合の再発有無

監査論点の再発は、コード上は未確認です（再発なし判定）。
ユーザー起点のJWT検証は index.ts:86 で必須化され、所有者チェックも index.ts:134 で実施されています。
内部起点専用化は index.ts:24, index.ts:14, index.ts:111 で有効です。
共通ガード実装は auth.ts:12-69 で確認済みです。
副作用候補

内部呼び出しは service role か cron secret 必須になったため、手動直叩き運用は401化します（意図どおり）。
threads-auto-reply へ誤った userId を渡す呼び出しは403になります（index.ts:50）。
既存の内部呼び出し元は service role クライアント経由で整合しており、破壊的な不整合は見つかっていません（index.ts:921, index.ts:871, index.ts:415, index.ts:397）。
追加で必要な最小修正

必須コード修正は現時点で不要です。
最小追加として、Edge Function回帰用に「未認証401 / 所有者不一致403 / 内部認証成功200」の3ケースを自動化できるテスト基盤（HTTPレベル）を1本だけ追加すると、今後の再発検知が安定します。
回帰確認結果

実行確認1: npx playwright test [token-validation.spec.ts](http://_vscodecontentref_/19) --project=chromium --reporter=line は 5/5 pass。
実行確認2: auth/persona/token-validation 合計15件は 9 pass / 6 fail。失敗はUI要素検出や/auth遷移期待の不一致で、今回のEdge Function認証修正の直接影響範囲外です（例: auth.spec.ts:17, persona.spec.ts:39）。
変更対象と隣接関数の診断は概ね問題なし（Deno URL import警告はVS Code側解決環境差分）。
この修正を閉じてよいか

監査重要論点（認証欠如/外部起動可能性）に限定すれば、クローズして問題ありません。
ただし「隣接UIテストの赤」を同時にゼロ化したい運用なら、別チケットで auth/persona E2Eの期待値調整を切り出してから閉じるのが安全です。