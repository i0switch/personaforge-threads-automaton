PersonaForge Threads Automaton 不具合監査レポート整理版 1. 監査サマリ 対象 personaforge-threads-automaton リポジトリ全体 主対象: Supabase Edge Functions バックエンドAPI 自動化ループ OAuth / Webhook / CRON系フロー システム概要 項目 内容 フロントエンド React + Vite + TypeScript バックエンド Supabase Edge Functions DB Supabase PostgreSQL 外部API Threads Graph API / Gemini / Resend 認証 Supabase Auth + JWT / CRON_SECRET / admin RPC デプロイ先 threads-genius-ai.lovable.app Supabase Project ID tqcgbsnoiarnawnppwia 主要自動化フロー 自動投稿 auto-post-generator → auto-scheduler → threads-post Webhook自動返信 threads-webhook → threads-auto-reply ポーリング返信チェック check-replies / webhook-reply-check → 自動返信 トークン更新 refresh-threads-tokens スタック復旧 process-unhandled-replies / fix-stuck-ai-replies / reset-stuck-ai-replies 2. エグゼクティブサマリ 確認できた最重要リスク この監査では、次の2系統が特に重大です。 A. 認証・権限制御の崩壊 一部Edge Functionで 呼び出し元の真正性確認が不足しており、設定次第では外部から不正実行される可能性があります。 特に generate-auto-reply / threads-auto-reply は、JWT検証なしでリクエストボディの userId / personaId を信用しているとされており、事実であれば 他人のAPIキーやThreadsトークンを使った不正投稿 に直結します。 B. 自動化処理の競合・ループ 投稿生成・返信処理・復旧処理が複数経路から走る一方で、事前ロック・排他制御・状態遷移の原子性が不十分です。 その結果、重複投稿・重複返信・無限ループ・ステータス不整合が起こりうる設計です。 総評 本番停止判断レベルのP0/P1が複数存在します。 特に 秘密鍵漏洩 認証不備 ボット間ループ 自動投稿の重複生成 は、運用事故ではなく本番障害・アカウント停止・不正利用事故に直結する論点です。 一方で、P2には デッドコード パフォーマンス課題 保守性低下 も含まれています。 3. 確定した重大問題の統合整理 3-1. 認証欠如による不正AI生成・Threads投稿 対象: generate-auto-reply, threads-auto-reply 深刻度: P0 / Critical 概要 これらの関数は verify_jwt = false の運用時に、外部から到達可能になります。 しかもコード内部でJWTの真正性を検証せず、リクエストボディの userId / personaId をそのまま信用しているとされています。 実害 他人のGemini API利用枠を勝手に消費 他人のThreadsアクセストークンを使った不正返信 Reputation毀損 Threadsアカウント停止リスク 実質的なアカウント乗っ取りに近い被害 根本原因 verify_jwt 設定への依存 関数内での supabase.auth.getUser(token) 等による認証未実装 リクエストボディの識別子を信頼している リリース判断 即停止レベル 3-2. 自動投稿生成のレースコンディション 対象: auto-post-generator 深刻度: P0〜P1上位 概要 「投稿時刻に達したか」を確認してから Gemini で文面生成し、投稿キューへinsertし、その後で next_run_at 等を更新しているため、遅いAI生成中に並列実行が重なると、同じ設定が複数ワーカーに拾われます。 実害 同一内容または重複投稿の大量生成 一時的なDB更新失敗で次分にも再実行 スパム投稿 Meta側のスパム検知・アカウント停止 根本原因 ロック取得が遅い 「生成前のclaim」がない DBレベルの排他制御不足 リリース判断 本番投入前に修正必須 3-3. 秘密鍵漏洩 対象: マイグレーションSQL 深刻度: P0 / Critical 概要 複数のmigrationファイルに service_role key が平文埋め込みされているとの指摘です。 anon key は公開前提ですが、service_role key はRLS完全バイパス権限を持つため、リポジトリアクセス可能者全員にDB全権限を配るのと同義です。 実害 全テーブル読み取り・更新・削除 シークレット取得 トークン改ざん 監査ログ破壊 完全なバックエンド侵害 リリース判断 最優先で鍵ローテーション 3-4. 複数Edge Functionの認証欠如 対象: auto-post-generator, fix-stuck-ai-replies, reset-stuck-ai-replies, check-replies, webhook-reply-check, self-reply-processor, fetch-threads-user-ids など 深刻度: P0〜P1 概要 config.toml 上は verify_jwt = true でも、本番設定が false の可能性があるとされています。 その状態で関数内部にCRON_SECRET/JWTチェックがない場合、外部から誰でも叩けます。 実害 全ユーザーへの一括処理を第三者が起動 stuck reply リセットによる再送爆発 全ペルソナの自動投稿強制実行 API/計算資源消耗 リリース判断 本番設定監査が即必要 3-5. ボット間返信ループ 対象: threads-webhook, check-replies, webhook-reply-check 深刻度: P0 概要 自己返信検出はあるものの不完全で、クロスペルソナの相互返信ループ防止がないとされています。 実害 Persona A → Persona B に返信 BのWebhookでAに返信 AのWebhookで再度Bに返信 以後ループ 影響 APIコール爆発 Threadsレート制限 スパム検知 アカウント停止 根本原因 自己判定が threads_username や persona.name に依存 bot-generated marker 不在 cross-persona防止ロジックなし 3-6. レート制限チェックのフェイルオープン 対象: threads-webhook, send-password-reset 深刻度: P0 概要 DBエラー時に「安全側で止める」のではなく、許可側に倒れる実装があるとされています。 実害 DDoS時に制限無効化 パスワードリセット乱発 Webhook濫用 リソース消耗 3-7. OAuth callback の state 未検証 対象: threads-oauth-callback 深刻度: P0 概要 クライアントでは state を見ていますが、サーバー側では code と persona_id しか受け取っていないため、CSRF防御がクライアント依存になっています。 実害 クライアントバイパス時の不正コールバック 任意ペルソナへのトークン結び付けの可能性 注意 有効な code の入手は前提であり、成立条件は限定的ですが、サーバー側未検証は不備です。 4. 優先度別の不具合一覧 4-1. P0 クリティカル ID 項目 要点 BUG-001 migrationにservice_role keyが平文埋め込み 鍵漏洩 BUG-002 複数Edge Functionに認証チェックなし 外部不正実行 BUG-003 ボット間返信ループ防止不完全 無限自動返信 BUG-004 レート制限がフェイルオープン DDoS/濫用耐性欠如 BUG-005 OAuth callbackでstate未検証 CSRF防御不足 追加重大論点 generate-auto-reply / threads-auto-reply でJWT未検証 他人のトークン悪用・不正投稿 4-2. P1 高優先 ID 項目 要点 BUG-006 トークンリフレッシュ非アトミック トークン喪失 BUG-007 check-replies と webhook-reply-check の競合 二重返信リスク BUG-008 webhook-reply-check の返信ステータス管理不全 pending放置・再処理 BUG-009 check-replies の processCount 初期チェックがデッドコード 実質無意味 BUG-010 auto-post-generator のタイムゾーン処理不整合 誤時刻投稿 BUG-011 threads-post で投稿済なのにDBがscheduledへ戻る 再投稿リスク BUG-012 self-reply-processor に認証なし 外部起動可能 追加重大論点 fetch-threads-user-ids に認証なし DoS / 無駄な外部API消費 追加重大論点 auto-post-generator で事前ロックなし 重複生成・無限自動化事故 4-3. P2 改善推奨 ID 項目 要点 BUG-013 Realtimeがスタブ化 リアルタイム機能不全 BUG-014 iOS Safariで10秒ポーリング固定 不要API呼び出し BUG-015 admin-reset-password が listUsers() 全件取得 スケーラビリティ低下 BUG-016 isEncrypted() が短い暗号文を誤判定しうる 暗号判定エッジケース BUG-017 brute force判定の実装が混在 セキュリティ実装の一貫性不足 BUG-018 config.toml と本番 verify_jwt 設定が乖離 ガバナンス不備 BUG-019 redirect_uri をクライアント入力から受理 防御深度不足 BUG-020 CLSの閾値評価ロジック不適切 計測品質低下 5. 根本原因ベースの統合 5-1. 認証・認可モデル崩壊 対象: BUG-002 / BUG-005 / BUG-012 / generate-auto-reply 論点 / fetch-threads-user-ids 論点 根本原因 verify_jwt に依存しすぎ 関数内部で認証検証しない CRON系とユーザー起点系の境界が曖昧 userId / personaId をリクエストボディから信用 まとめ 「到達できること」と「実行してよいこと」の区別が甘いです。 5-2. 自動化ワーカーの排他制御不足 対象: BUG-003 / BUG-007 / BUG-008 / 追加 auto-post-generator 論点 / BUG-011 根本原因 slow API call前にclaimしない 状態更新が後置き 複数経路から同じ対象を処理 送信/保存/状態更新が不可分でない まとめ 「非同期自動化を複数ルートで動かす設計」に対し、排他制御と状態遷移の設計が追いついていないです。 5-3. 秘密情報と設定管理のガバナンス不足 対象: BUG-001 / BUG-018 / BUG-019 根本原因 migrationに秘密情報を埋め込み 本番/ローカル設定差分の可視化不足 OAuth許可設定のサーバー側拘束不足 5-4. フェイルセーフ設計不足 対象: BUG-004 / BUG-006 / BUG-011 / BUG-017 根本原因 DBエラー時に許可側に倒れる 外部API成功後のDB失敗を吸収できない 「止めるより続ける」実装が多い 6. 判定保留・未確認事項 以下は**不明（要追加確認）**です。現時点では断定不可です。 ID 項目 保留理由 AMB-001 THREADS_WEBHOOK_SECRET が正しいか 環境変数実値未確認 AMB-002 owner_id / author_id が返信オブジェクトに含まれるか Threads API仕様確認不足 AMB-003 process-unhandled-replies 後半に追加バグがあるか 全文未精査 AMB-004 RLSポリシー網羅確認 Supabase側未接続 AMB-005 retrieve-secret の内部実装 未読 AMB-006 req.clone().json() のエラーハンドリング妥当性 実装仕様依存 追加保留 本番の verify_jwt 実値 config.tomlと乖離の可能性のみで未実測 追加保留 service_role旧キー失効状況 鍵ローテーション状況未確認 7. テストギャップ E2Eテストで不足している領域 Threads OAuthフロー 自動投稿の設定→生成→投稿反映 Webhook受信→自動返信 トークン期限切れからの回復 管理者パスワードリセット レート制限時のUI表示 Edge Functionテスト不足 自動テストがない .http ファイル中心で手動検証依存 CRON関数の統合試験なし 未テスト重要パス クロスペルソナループ stuck reply復旧後の重複送信 トークン更新中のDB失敗 Threadsレート制限到達時の停止動作 migration順序整合性 8. 最終リリース判断 今すぐ止めるべき事項 以下は本番リリース阻害ありです。 BUG-001 service_role key 漏洩 BUG-002 Edge Function認証欠如 generate-auto-reply / threads-auto-reply のJWT未検証 他ユーザーなりすまし投稿 auto-post-generator の事前ロックなし 重複生成・無限投稿事故 BUG-003 ボット間ループ BUG-004 レート制限フェイルオープン BUG-005 OAuth stateサーバー未検証 BUG-011 投稿済みなのにDBがscheduledへ戻る 次段階で修正すべき事項 BUG-006 トークン消失リスク BUG-007 / BUG-008 返信処理競合 BUG-012 self-reply-processor認証不足 fetch-threads-user-ids バッチジョブ外部叩き放題問題 BUG-010 タイムゾーン処理不整合 後回し可能な事項 BUG-009 processCountの初期デッドコード BUG-013〜020 P2群 一部の保守性・観測性改善 9. 推奨対応順 即日 service_role key のローテーション migrationから秘密情報除去 本番 verify_jwt 実値の監査 全Edge Functionに JWT検証 CRON_SECRET検証 所有者チェック を追加 generate-auto-reply / threads-auto-reply を即遮断または修正 当週 auto-post-generator を生成前claim方式に変更 ボット間ループ防止 reply_depth bot marker cross-persona禁止 OAuth callback のサーバー側 state 検証 レート制限フェイルクローズ化 threads-post の公開済み/DB不整合対策 次週 返信処理系の状態遷移統一 トークン更新処理の原子性向上 Edge Function統合テスト追加 E2Eの主要業務フロー追加