回帰チェック結果

再発なし: 既知論点（競合経路・ループ防止・返信ステータス不整合）に対する直前修正は、影響範囲の静的確認で崩れていません。
追加最小修正を適用: index.ts:25-27 の判定を型安全化し、internalAuth.response の型警告要因を解消しました（挙動変更なし）。
副作用候補: index.ts と index.ts はエラーなし、隣接の復旧系ロジックとも整合しています。
未解消の警告（環境由来）

index.ts と index.ts の Deno URL import / Deno グローバル未解決は、ローカルTS解析環境起因で今回修正範囲外です。
クローズ判定

監査指定の回帰観点ではクローズ可能です。必要なら次に、対象4関数だけを使った最小の統合実行確認手順（手動チェック項目）を作成しますか。