回帰確認結果

対象論点は BUG-005（OAuth callback state未検証）と BUG-006（token refresh非アトミック）に限定して確認。
実装確認で BUG-005 の再発は未検出。state必須化・期限検証・一致検証・ワンタイム消費が実装済み（index.ts:32-205）。
実装確認で BUG-006 の再発は未検出。CAS型の条件付き更新で競合時は上書きせずスキップする実装に変更済み（index.ts:8-191）。
隣接フロー（OAuth開始/完了画面）も整合。state保存・callback引き渡しを確認（ThreadsOAuthButton.tsx:57-74, ThreadsOAuthCallback.tsx:52）。
ビルドは成功（npm run build）。限定E2Eは 15件中 9件成功/6件失敗。失敗は認証ページ描画・遷移前提の既存系で、今回修正の直接影響は確認されず（失敗箇所: auth.spec.ts:17, auth.spec.ts:23, auth.spec.ts:40, auth.spec.ts:76, persona.spec.ts:39, persona.spec.ts:45）。
元不具合の再発有無

BUG-005: 再発なし（サーバー側state検証あり）。
BUG-006: 再発なし（refresh成功後のみ反映 + 旧値一致条件で原子更新）。
副作用候補

migration未適用環境では OAuth開始時のstate保存が失敗する可能性（20260303090000_add_oauth_state_columns_to_personas.sql:1-12）。
競合時に refresh が「失敗（スキップ）」として計上されるため、監視上の失敗率が見かけ上増える可能性（動作としては安全側）。
state有効期限超過時に再認証要求が増える可能性（仕様どおりの厳格化）。
追加で必要な最小修正

回帰実行継続のために最小修正を1件実施済み: JSON破損を修正（tsconfig.app.json:18）。
今回論点に対して追加のコード修正は不要。
この修正を閉じてよいか

閉じてよいです（条件付きで YES）。
クローズ条件は、上記migrationが全環境に適用済みであること、E2E失敗6件（認証前提系）を別チケットで切り分けることです。
